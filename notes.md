# Python data structures

A class is an object which contains data that we call attributes, and also contains some functionality that we call methods. 

A class is like a template to create objects, and the objects created from that class is called instances 